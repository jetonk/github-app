import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btndrawerOpen: {
    color: '#ffffff',
  },
  inputSearch: {
    width: '70%',
  },
  errorMsg: {
    color: 'red',
  },
});
export default styles;
